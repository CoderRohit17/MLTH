import 'package:flutter/material.dart';

class DetailSelectionPage extends StatelessWidget {
  const DetailSelectionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
